# ============================================================
# .env.example — Skopiuj do .env.local i uzupełnij wartości
# NIGDY nie commituj .env.local do GitHuba!
# ============================================================

# ---- Supabase ----
SUPABASE_URL=https://TWOJ_PROJEKT.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGci...            # Settings → API → service_role (TAJNY!)
NEXT_PUBLIC_SUPABASE_URL=https://TWOJ_PROJEKT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...  # Settings → API → anon/public

# ---- AI API Keys (klucze tylko na serwerze!) ----
ANTHROPIC_API_KEY=sk-ant-api03-...
OPENAI_API_KEY=sk-proj-...

# ---- Stripe ----
STRIPE_SECRET_KEY=sk_live_...              # lub sk_test_... do testów
STRIPE_WEBHOOK_SECRET=whsec_...            # Stripe → Webhooks → signing secret

# Price IDs — skopiuj z Stripe Dashboard → Products → każdy produkt → Price ID
STRIPE_PRICE_STARTER=price_...             # 40 zł/mies  — 500k tokenów/dzień
STRIPE_PRICE_MINI_DEVELOPER=price_...      # 80 zł/mies  — 1,25M tokenów/dzień
STRIPE_PRICE_DEVELOPER=price_...           # 160 zł/mies — 3M tokenów/dzień
STRIPE_PRICE_GIGA_DEVELOPER=price_...      # 350 zł/mies — 7M tokenów/dzień

# ---- App ----
NEXT_PUBLIC_APP_URL=https://twojadomena.vercel.app   # lub http://localhost:3000
